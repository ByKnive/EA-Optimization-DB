# AlgoTesting-Dashboard
Structuring your different bots, the optimization runs and set parameters that are then visualized into a dashboard.

Example command for bash to import data manually:
python .\mt5_import.py '.\Exports\GU-LondonBreakout-1.xlsx' trading.db --bot-name 'LondonBreakout' --version v3
